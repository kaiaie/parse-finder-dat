#include <stdlib.h>
/// \file parser.c
/// \brief Implements the FINDER.DAT parsing functionality

/// \brief Parses the file in the specified directory
/// \return 0 if file can be parsed, non-zero otherwise
int parse(const char *dir)
{
	return EXIT_SUCCESS;
}
